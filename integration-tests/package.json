{
  "name": "hyper-pi-integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-component integration tests for Hyper-Pi",
  "scripts": {
    "test": "vitest run --exclude src/e2e-live.test.ts",
    "test:e2e-live": "vitest run src/e2e-live.test.ts",
    "test:all": "vitest run",
    "test:watch": "vitest --exclude src/e2e-live.test.ts"
  },
  "devDependencies": {
    "@types/ws": "^8.5.0",
    "@vitest/coverage-v8": "^4.0.18",
    "vitest": "^4.0.18",
    "ws": "^8.18.0"
  }
}
