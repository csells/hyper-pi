# Cloudflare Tunnel config for remote Pi-DE access.
# Copy to .env and fill in your values. (.env is gitignored)
#
# One-time setup: bash .pi/skills/cf-tunnel/scripts/setup.sh
# Daily start:    bash .pi/skills/cf-tunnel/scripts/start.sh

CF_TUNNEL_NAME=hyper-pi-yourname
PIDE_HOSTNAME=pide.yourdomain.com
HYPI_HOSTNAME=hypi.yourdomain.com
HYPI_TOKEN=change-me-to-a-real-secret

# Optional: explicit path to tunnel credentials JSON.
# Auto-discovered from CF_TUNNEL_NAME if omitted.
# CF_CREDENTIALS_FILE=~/.cloudflared/<tunnel-id>.json
